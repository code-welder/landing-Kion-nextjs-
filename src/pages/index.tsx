import Head from 'next/head';
import { NextPage } from 'next';

const Home: NextPage = () => {
  return (
    <>
      <Head>
        <title>Kion</title>
        <meta name='viewport' content='width=device-width, initial-scale=1.0' />
        <meta name='description' content='We deliver more than just the features to manage and govern your cloud' />
      </Head>
    </>
  );
};

export default Home;
